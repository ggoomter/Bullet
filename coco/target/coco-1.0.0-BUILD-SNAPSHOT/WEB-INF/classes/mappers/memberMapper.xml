<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">


 <!-- org.apache.ibatis.reflection.ReflectionException: There is no getter for property named 'ID' in 'class yjc.wdb.domain.Member' MAPPER의 #{}안의 대소문자와 DTO(VO)의 대소문자가 달라서 그럴확률이 90% -->
	<select id="Id_Check" resultType="int">
		select count(*) from MEMBER U
		where U.ID = #{id}
	</select>

	<insert id='Register'>
		INSERT INTO MEMBER (ID, PW)
		VALUES (#{id}, #{pw})
	</insert>

	<select id="Login" resultType="int">
		select count(*) from MEMBER U
		where U.ID = #{id} and U.PW = #{pw}
	</select>

	<select id="Login_Info" resultType="Member">
		select * from MEMBER U
		where U.ID = #{id} and U.PW = #{pw}
	</select>
</mapper>